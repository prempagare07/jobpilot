# JobPilot Frontend

This is the Next.js 14 App Router dashboard for JobPilot. It uses Tailwind CSS and shadcn-style UI primitives in `components/ui`.

Run it from the project root with:

```bash
make dev-frontend
```

Or from this directory:

```bash
npm run dev
```

The backend API is expected at `http://localhost:8000`.
